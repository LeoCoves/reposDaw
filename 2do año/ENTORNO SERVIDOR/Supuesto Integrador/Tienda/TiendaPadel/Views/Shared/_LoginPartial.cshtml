@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager



<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
    @if (User.IsInRole("Usuario"))
    {
            <li class="nav-item">
                <a class="nav-link color-terciario fuente-editorial text-center" asp-area="" asp-controller="MisPedidos" asp-action="Index">ORDERS</a>
            </li>
        <li class="nav-item d-flex justify-content-between">
                <a class="nav-link color-terciario fuente-editorial text-center" asp-area="" asp-controller="MisDatos" asp-action="Edit">
                    <strong><i class="bi bi-person-circle"></i></strong>
                </a>

            @*<a class="nav-link text-dark text-center" asp-area="" asp-controller="Carrito" asp-action="Index">
                
            </a>*@

                <button class="nav-link btn btn-link color-terciario fuente-editorial text-center" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasCarrito">
                    <strong><i class="bi bi-bag"></i></strong>
                </button>
        </li>
    }
    <li class="nav-item d-flex justify-content-center">
        <form  class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button type="submit" class="nav-link btn btn-link color-terciario fuente-editorial text-center">
                    <strong><i class="bi bi-box-arrow-right"></i></strong>
            </button>
                
        </form>
    </li>
}
else
{
    @*<li class="nav-item">
            <a class="nav-link text-dark text-center" asp-area="Identity" asp-page="/Account/Register">Registrate</a>
    </li>
    *@
        <li class="nav-item  d-flex justify-content-between">
            <a class="nav-link btn btn-link color-terciario  text-center" asp-area="" asp-controller="Carrito" asp-action="Index">
                <strong><i class="bi bi-bag"></i></strong>
            </a>

            <a class="nav-link btn btn-link color-terciario text-center" asp-area="Identity" asp-page="/Account/Login">
                <strong><i class="bi bi-box-arrow-left"></i></strong>
            </a>
    </li>
}
</ul>

